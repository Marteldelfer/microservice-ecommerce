services:
  product_service:
    build:
      context: ./product_service
      dockerfile: Dockerfile
    env_file:
      - ./keys/awsKeys
    container_name: product_service
    ports:
      - "8000:8000"
    networks:
      - internal

networks:
  internal:
    driver: bridge
